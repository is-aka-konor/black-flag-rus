<section class="summary">
	<figure class="poster">
		<img src="{{ ifThen editable img.editable item.img }}" alt="{{ item.name }}"
				 {{~#if editable}} data-action="editImage" data-edit="img"{{else}} data-action="showIcon"{{/if}}>
		{{#if editable}}
			{{ formInput name.field value=name.editable classes="document-name" }}
		{{else}}
			<div class="document-name">{{ item.name }}</div>
		{{/if}}
		{{#if (and editable (eq item.type "class"))}}
			<div class="color">
				<input name="system.color" type="color" value="{{ system.color }}"
				       data-tooltip aria-label="{{ localize 'BF.AccentColor.Label' }}">
			</div>
		{{/if}}
		<div class="source">
			{{ system.description.source.label }}
		</div>
		<div class="banner-mask" inert></div>
	</figure>
	<div class="info drop-target">
		<div class="description flexcol">
			{{#if editable}}
				<textarea name="system.description.short">{{ system.description.short }}</textarea>
			{{else}}
				<p>{{ system.description.short }}</p>
			{{/if}}
			{{#if showClassSelector}}
				{{ formField fields.identifier.fields.class value=source.identifier.class choices=CONFIG.registration.all.class
				             labelAttr="name" classes="label-adjacent" hint="" localize=true rootId=partId }}
			{{/if}}
			<div class="drop-area journal-link">
				{{#if system.description.journal}}
					<p>
						<a class="content-link" data-link="{{ system.description.journal }}"
						   data-uuid="{{ system.description.journal }}">
							<i class="fa-solid fa-book-open" inert></i>
							{{ localize "BF.Journal.Link.MoreInformation" }}
						</a>
						{{#if editable}}
							<button class="link-button" type="button" data-action="deleteJournalLink"
							        data-tooltip="BF.Item.Journal.Unlink" aria-label="{{ localize 'BF.Item.Journal.Unlink' }}">
								<i class="fa-solid fa-trash" inert></i>
							</button>
						{{/if}}
					</p>
				{{else if editable}}
					<p class="hint">{{ localize "BF.Journal.Link.DropArea" }}</p>
				{{/if}}
			</div>
		</div>
		<ul class="traits">
			{{#each system.traits as |trait|}}
				<li class="trait">
					{{ localize trait.label }}: {{ trait.value }}
				</li>
			{{/each}}
		</ul>
	</div>
</section>
