<section>
	<div class="separator">
		<h3>{{ localize "BF.Rule.Tooltip" }}</h3>
		<label>
			{{ localize "BF.Rule.Type.Label" }}
			<select name="system.type">
				{{ selectOptions CONFIG.ruleTypes selected=document.system.type labelAttr="label" localize=true }}
			</select>
		</label>
	</div>
	<prose-mirror name="system.tooltip" value="{{ source.system.tooltip }}" class="journal-page-content"></prose-mirror>
</section>
