<fieldset>
	<legend class="hinted-label" data-tooltip="BF.Advancement.Size.FIELDS.options.hint">
		{{ localize "BF.Advancement.Size.FIELDS.options.label" }}
	</legend>
	<ol class="option-list">
		{{#each sizes}}
			<li>
				<label>
					{{ localize label }}
					<input type="checkbox" name="configuration.options.{{ @key }}" {{ checked selected }}>
				</label>
			</li>
		{{/each}}
	</ol>
</fieldset>
