<fieldset>
	<legend>{{ localize "BF.Advancement.Config.Details" }}</legend>
	{{> "blackFlag.advancement-controls" }}
	{{ formField configuration.fields.mode value=configuration.data.mode options=CONFIG.traitModes.localizedOptions
	             hint=(localize (lookup (lookup CONFIG.traitModes configuration.data.mode) "hint")) rootId=partId }}
</fieldset>
