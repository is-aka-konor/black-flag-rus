<fieldset>
	<legend>{{ localize "BF.Advancement.KeyAbility.FIELDS.options.label" }}</legend>
	<ol class="option-list">
		{{#each abilities}}
			<li>
				<label>
					{{ localize label }}
					<input type="checkbox" name="configuration.options.{{ @key }}" {{ checked keySelected }}>
				</label>
			</li>
		{{/each}}
	</ol>
</fieldset>
